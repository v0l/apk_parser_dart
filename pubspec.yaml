name: apk_parser
description: "Wrapper lib over a rust APK parsing lib"
version: 1.0.0+1

environment:
  sdk: ^3.6.2
dependencies:
  flutter:
    sdk: flutter
  rust_lib_apk_parser:
    path: rust_builder
  flutter_rust_bridge: 2.7.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
  integration_test:
    sdk: flutter

flutter: